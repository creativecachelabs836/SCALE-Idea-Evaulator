node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# Local data + secrets
.env
.env.local
# Anchored: an unanchored `data/` also matches `src/data/`, which silently
# excluded the entire persistence layer from the V1 commit.
/data/
*.db
*.db-journal
*.db-wal
*.db-shm

.DS_Store
